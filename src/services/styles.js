const getAll = () => {
    const response = [
        { id: 1, name: 'Weizen' },
        { id: 2, name: 'IPA' }
    ]
    return response
}

const stylesService = {
    getAll
}

export default stylesService